import React from 'react';
import './index.css';

const CompleteComponent = (props) => {
    return (
        <div className="complete-container">
            <div className="complete-container-title">Delegations Completed!</div>
        </div>
    );
}

export default CompleteComponent;